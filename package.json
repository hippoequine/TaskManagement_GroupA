{
  "name": "taskmanagement_a",
  "version": " 0.0.1",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "docker:up": "docker compose up --build",
    "keycloak-save-config": "docker exec $(docker ps -f name=keycloak -q) /opt/keycloak/bin/kc.sh export --realm taskmanager --dir /opt/keycloak/data/import",
    "docker:down": "docker compose down -v",
    "docker:clean": "docker compose down --rmi all --volumes --remove-orphans",
    "prettier:check": "prettier --check .",
    "prettier:write": "prettier --write .",
    "ci:all": "npm ci && npm ci --prefix frontend && npm ci --prefix backend",
    "test": "vitest run --config backend/vitest.config.js && vitest run --config frontend/vitest.config.js",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --config backend/vitest.config.js --coverage && vitest run --config frontend/vitest.config.js --coverage",
    "test:integration:frontend": "vitest run --config frontend/vitest.integration.config.js",
    "test:integration:backend": "vitest run --config backend/vitest.integration.config.js",
    "test:integration": "npm run test:integration:frontend && npm run test:integration:backend",
    "lint": "npm run lint --prefix frontend && npm run lint --prefix backend"
  },
  "devDependencies": {
    "@testing-library/user-event": "^14.6.1",
    "@vitest/coverage-v8": "^4.0.18",
    "jsdom": "^28.1.0",
    "prettier": "^3.7.4",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@mui/x-date-pickers": "^8.27.2",
    "dayjs": "^1.11.19",
    "multer": "^2.1.0",
    "supertest": "^7.2.2",
    "uuid": "^13.0.0",
    "yaml": "^2.8.2"
  }
}
